# Artwork

Drop image files (`.jpg`, `.jpeg`, `.png`, `.webp`, `.avif`) into this folder and they'll
appear automatically on the **/artwork** page — optimized by Astro, laid out as a masonry grid,
sorted by filename. No code changes needed; just add files and redeploy (`npm run deploy`).
